<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.pwr.projekt.enduroracepilot.activities.BrowseRouteActivity">

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:layout_marginTop="8dp"
        android:layout_centerInParent="true"
       />
<TextView
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_above="@+id/routeListARA"
    android:text="Select Route"
    android:textSize="24sp"
    android:textAlignment="center"
    android:layout_margin="0dp"
    />
    <ListView
        android:id="@+id/routeListARA"
        android:layout_width="368dp"
        android:layout_height="495dp"
        android:layout_marginLeft="8dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:layout_above="@+id/createRouteButton"
        android:layout_marginBottom="8dp"/>
    <Button
        android:id="@+id/createRouteButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:layout_margin="8dp"
        tools:layout_editor_absoluteY="447dp"
        tools:layout_editor_absoluteX="160dp"
        android:layout_alignParentBottom="true"
        android:text="Create new route"/>
</RelativeLayout>
